---
type Props = {
  id?: string;
  title: string;
  subTitle?: string;
};

const { props } = Astro;
---

<h2 id={props.id}>
  {props.title}{!!props.subTitle && <small>{props.subTitle}</small>}
</h2>

<style>
  h2 {
    align-items: baseline;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  small {
    color: var(--gray-400);
    display: inline;
    font-size: 50%;
  }
</style>
