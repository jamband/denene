---
type Props = {
  class?: string;
};

const { class: className, ...attributes } = Astro.props;
---

<section class={className} {...attributes}><slot /></section>

<style>
  section {
    margin-bottom: 3.5rem;
  }
</style>
