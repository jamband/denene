---
type Props = {
  code: string;
  inline?: boolean;
};

const { code, inline } = Astro.props;
---

{
  inline ? (
    <code>{code}</code>
  ) : (
    // prettier-ignore
    <pre><code>{code}</code></pre>
  )
}
