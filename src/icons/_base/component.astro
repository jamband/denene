---
type Props = {
  viewBox: string;
  class: string;
};

const { viewBox, class: className } = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  fill="currentColor"
  role="img"
  aria-hidden={true}
  viewBox={viewBox}
  class={className}
  ><slot />
</svg>
